<Page x:Class="Tai.UI.Views.Overview"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Tai.UI.Views" xmlns:input="clr-namespace:LancerUI.Controls.Input;assembly=LancerUI" xmlns:buttons="clr-namespace:LancerUI.Controls.Buttons;assembly=LancerUI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Overview">

    <Grid>
        <TextBlock Text="{Binding Content}"/>
        <input:LUInput Margin="0,20,0,0" Placeholder="hello world" Text="{Binding Content,Mode=TwoWay}"></input:LUInput>
        <buttons:LUButton Margin="0,40,0,0" Width="120" Command="{Binding TestCommand}">Go to page1</buttons:LUButton>
    </Grid>
</Page>
